# 子系统集成流程图

---

## 一、系统总体集成架构

```mermaid
flowchart TB
    subgraph S1["子系统一：多源数据采集（严励）"]
        direction TB
        A1["大都会博物馆<br/>REST API 爬虫"] --> A4
        A2["亚洲艺术博物馆<br/>Playwright 浏览器爬虫"] --> A4
        A3["费城艺术博物馆<br/>POST API + 多线程爬虫"] --> A4
        A4["增量爬取框架<br/>指纹比对 + 变更日志"]

        A1 -.- A1d["19,901 条"]
        A2 -.- A2d["3,053 条"]
        A3 -.- A3d["2,500 条"]
    end

    subgraph S2["子系统二：数据预处理与质量保障（张前宇）"]
        direction TB
        B1["清洗流水线<br/>完全去重→无效过滤→字段完整性→URL去重→朝代匹配"] --> B2
        B2["翻译模块<br/>百度API + 断点续传"] --> B3
        B3["质量检测<br/>106 项自动化测试"]
    end

    subgraph S3["子系统三：三元组构建与知识存储（王卓航）"]
        direction TB
        C1["MySQL 关系型存储<br/>6 表结构化存储"] --> C2
        C2["Neo4j 知识图谱<br/>4 类节点 + 3 类关系"]
    end

    S1 -->|"25,454 条原始 CSV"| S2
    S2 -->|"6,243 条标准化 CSV<br/>统一 20 列 schema"| S3
```

---

## 二、子系统一详细流程：多源数据采集

```mermaid
flowchart TB
    subgraph Met["大都会艺术博物馆"]
        M1["关键词搜索 API<br/>China + Chinese"] --> M2["获取 ObjectID 列表"]
        M2 --> M3["逐条请求详情接口<br/>限速：5次/秒"]
        M3 --> M4["断点续写 CSV<br/>每10条追加"]
        M4 --> M5["失败ID追踪<br/>failed_ids.csv"]
    end

    subgraph Asian["亚洲艺术博物馆"]
        A1["Playwright 启动浏览器"] --> A2["分页遍历列表页"]
        A2 --> A3["提取详情页链接"]
        A3 --> A4["逐页解析详情<br/>BeautifulSoup"]
        A4 --> A5{"验证码拦截？"}
        A5 -->|"是"| A6["暂停等待人工处理"]
        A6 --> A4
        A5 -->|"否"| A7["保存数据"]
    end

    subgraph Phila["费城艺术博物馆"]
        P1["POST API 分页请求<br/>每页48条"] --> P2["ThreadPoolExecutor<br/>10线程并发"]
        P2 --> P3["UUID去重 + 详情获取"]
        P3 --> P4["失败UUID自动重试<br/>Session级3次重试"]
    end

    Met --> R["原始 CSV 文件"]
    Asian --> R
    Phila --> R

    R --> Inc["增量爬取框架"]
    Inc --> Inc1["IncrementalTracker<br/>MD5指纹比对"]
    Inc --> Inc2["CrawlLogger<br/>变更日志记录"]
    Inc --> Inc3["IncrementalCSVWriter<br/>增量写入"]
```

---

## 三、子系统二详细流程：数据预处理与质量保障

```mermaid
flowchart TB
    subgraph Clean["清洗流水线"]
        CL1["Step1: 完全重复去重"] --> CL2["Step2: 无效记录过滤<br/>无图片/无详情链接"]
        CL2 --> CL3["Step3: 关键字段完整性<br/>title/period/image/detail_url"]
        CL3 --> CL4["Step4: 详情页URL去重<br/>统一去重策略"]
        CL4 --> CL5["Step5: 朝代匹配<br/>年份→朝代ID映射"]
        CL5 --> CL6["Step6: 标准化输出<br/>统一20列schema"]
    end

    subgraph Translate["翻译模块"]
        TR1["百度翻译API批量翻译"] --> TR2["断点续传<br/>加载已有进度"]
        TR2 --> TR3["翻译字段<br/>title/type/material/description"]
    end

    subgraph QA["质量检测"]
        QA1["文件完整性 25项"] --> QA7
        QA2["数据质量 21项"] --> QA7
        QA3["ID一致性 8项"] --> QA7
        QA4["代码质量 36项"] --> QA7
        QA5["翻译质量 3项"] --> QA7
        QA6["Neo4j完整性 2项"] --> QA7
        QA7["106项自动化测试<br/>通过率 100%"]
    end

    Clean -->|"6,243条"| Translate
    Translate -->|"6,243条<br/>零数据丢失"| QA
```

---

## 四、子系统三详细流程：三元组构建与知识存储

```mermaid
flowchart TB
    subgraph MySQL["MySQL 关系型存储"]
        DB1["museums 博物馆表<br/>3条记录"] --> DB7
        DB2["dynasties 朝代表<br/>28条记录"] --> DB7
        DB3["artists 艺术家表"] --> DB7
        DB4["artifacts 文物主表<br/>6,243条记录"] --> DB7
        DB5["artifact_images 图片表"] --> DB7
        DB6["artifact_artist 关联表"] --> DB7
        DB7["断线重连 + 批量提交<br/>失败3次重试"]
    end

    subgraph Neo4j["Neo4j 知识图谱"]
        N1["Artifact 文物节点<br/>6,243个"] --> N5
        N2["Museum 博物馆节点<br/>3个"] --> N5
        N3["Dynasty 朝代节点<br/>28个"] --> N5
        N4["Artist 艺术家节点"] --> N5
        N5["三类关系三元组"]

        N1 -->|"COLLECTED_BY"| N2
        N1 -->|"BELONGS_TO"| N3
        N1 -->|"CREATED_BY"| N4
    end

    MySQL -->|"CSV导出"| Neo4j
```

---

## 五、子系统集成接口与数据流

```mermaid
flowchart LR
    subgraph 接口1["集成接口1：原始CSV"]
        I1a["大都会: daduihui_en.csv<br/>8列, 19,901条"]
        I1b["亚洲艺术: objects_china_verified_dedup.csv<br/>11列, 3,053条"]
        I1c["费城: Philamuseum_..._final.csv<br/>12列, 2,500条"]
    end

    subgraph 接口2["集成接口2：标准化CSV"]
        I2a["daduhui_final.csv<br/>20列, 2,995条"]
        I2b["asian_final.csv / asian_final_translated.csv<br/>20列, 748条"]
        I2c["clean_artifacts.csv / clean_artifacts_translated.csv<br/>21列, 2,500条"]
    end

    subgraph 接口3["集成接口3：数据库CSV"]
        I3a["museums / dynasties / artists"]
        I3b["artifacts / artifact_images"]
        I3c["artifact_artist"]
    end

    接口1 -->|"采集→预处理"| 接口2
    接口2 -->|"预处理→存储"| 接口3
```

---

## 六、集成过程中遇到的问题与解决方案

| 序号 | 问题 | 涉及子系统 | 根因 | 解决方案 | 效果 |
|------|------|-----------|------|---------|------|
| 1 | 三馆 object_id 冲突 | 采集→预处理→存储 | 三馆独立编号，ID范围重叠 | 分段ID分配：费城1~2500，大都会2501~5495，亚洲艺术5527~6274 | 零冲突 |
| 2 | 清洗-翻译数据行数不一致 | 预处理内部 | 清洗和翻译独立运行，筛选条件不同 | 基于 detail_url 严格对齐，重新筛选清洗数据 | 2,995行完全对齐 |
| 3 | 大都会 type 字段100%为空 | 预处理→存储 | API未提供type字段 | 基于material智能推断引擎，覆盖9大文物类型 | 空值率100%→0.2% |
| 4 | 朝代匹配跨馆不一致 | 采集→预处理→存储 | 各脚本独立定义朝代映射 | 统一朝代表（clean_dynasties.csv），所有脚本引用同一映射 | 25条朝代完全一致 |
| 5 | 翻译中断需从头开始 | 预处理内部 | 无进度持久化 | 断点续传机制，加载已有翻译进度文件 | 中断后可继续 |
| 6 | 全量爬取效率低 | 采集→预处理 | 每次重新下载所有数据 | 增量爬取框架，MD5指纹比对仅处理变更 | 二次爬取仅处理新增/更新 |
| 7 | MySQL大批量写入断线 | 预处理→存储 | 网络不稳定导致连接中断 | 错误码检测+自动重连+每50条批量提交 | 写入成功率100% |
| 8 | 亚洲艺术含非中国文物 | 采集→预处理 | 爬虫按"China"关键词搜索，结果含日韩文物 | NON_CHINESE_PERIODS过滤器 + Department字段筛选 | 仅保留Chinese Art |
| 9 | 费城数据含HTML标签 | 预处理 | API返回的摘要字段含HTML | clean_html()函数，HTML转纯文本 | 标签全部清除 |

---

## 七、数据量流转统计

```mermaid
flowchart LR
    S["爬虫原始<br/>25,454条"] -->|"清洗"| C["清洗后<br/>6,243条"]
    C -->|"翻译"| T["翻译后<br/>6,243条"]
    T -->|"存储"| N["知识图谱<br/>6,243条"]

    S -.- S1["大都会: 19,901"]
    S -.- S2["亚洲艺术: 3,053"]
    S -.- S3["费城: 2,500"]

    C -.- C1["大都会: 2,995"]
    C -.- C2["亚洲艺术: 748"]
    C -.- C3["费城: 2,500"]
```

> **核心指标**：翻译和知识图谱阶段数据量与清洗后完全一致，**零数据丢失**。
